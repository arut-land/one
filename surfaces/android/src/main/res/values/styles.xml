<resources>
    <!-- The launch window only. Compose owns the app's colors and uses the
         device's Material You palette, so no accent is named here. -->
    <style name="Theme.Arut" parent="android:style/Theme.Material.Light.NoActionBar" />
</resources>
