# The same Gradle `mise.toml` pins, so a wrapper build and a mise build agree.
# `gradlew`, `gradlew.bat` and `gradle-wrapper.jar` are not checked in; run
# `mise exec -- gradle wrapper` once in this directory to materialize them.
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-9.7.1-bin.zip
networkTimeout=10000
validateDistributionUrl=true
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
