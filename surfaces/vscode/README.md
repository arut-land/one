# VS Code prototype

The existing prototype now composes scope handles and keeps navigation in the
surface. It lives at the target `surfaces/vscode` path. Native extension-host
hosting remains Phase 4 work; this prototype still loads the wasm package.
