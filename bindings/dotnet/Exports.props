<Project>
  <!-- Every generated FFI type reaches a consumer through the namespace itself,
       with no rename layer of our own to keep in sync (ADR 0007). -->
  <ItemGroup>
    <Using Include="Arut.Ffi" />
    <Using Include="Arut.Ffi.Arut_ffi" Static="true" />
  </ItemGroup>
</Project>
