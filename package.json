{
  "name": "arut",
  "private": true,
  "scripts": {
    "build": "pnpm --recursive --if-present build",
    "check": "tsc -p tsconfig.json && tsc -p tsconfig.node.json && node --test bindings/typescript/tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "@types/vscode": "^1.96.0",
    "typescript": "catalog:"
  }
}
